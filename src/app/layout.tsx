import "./globals.css";

export const metadata = {
  title: "GNPay Africa",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      {/* <title>Gnpay Africa</title> */}
      <body>{children}</body>
    </html>
  );
}
